[ ] Create dummy variable XP in useShopData

[ ] PROGRESS BAR:
[ ] 1. Create ProgressBarComponent
[ ] 2. Create function/computed nextUnlock returns (nextItem, nextXP)
[ ] 3. Create simple progress bar styling, with progress percentage = 
    (1 - (XP/NextXP)) * 100
[ ] 4. Display XP, nextXP, nextItem

[ ] CHANGE UNLOCK MECHANISM:
[ ] 1. In fishcomponent, change isFishAvailable and isHatAvailable with logic:
    if XP >= price.
[ ] 2. Some more styling in selectors
[ ] 3. Check all project files for "unlock*" and apply same logic if possible.

[ ] FEEDBACK BAR:

[ ] INTEGRATE EMILS POINT SYSTEM w. XP AND FEEDBACK BAR


